import { TimeContainer } from './Container';
import { TimeEmpty } from './Empty';
import { TimeForm } from './Form';
import { TimeTableList } from './Table';

export const TimeList = {
  Root: TimeContainer,
  Form: TimeForm,
  Table: TimeTableList,
  Empty: TimeEmpty,
}